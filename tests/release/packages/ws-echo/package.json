{
  "name": "perry-release-fixture-ws-echo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Tier-3 fixture: WebSocket echo round-trip via the `ws` npm package. Server + client are both in-process; tests perry-ext-ws's Server/Client surface end-to-end without requiring a network port to be exposed externally.",
  "dependencies": {
    "ws": "^8.18.0"
  }
}
