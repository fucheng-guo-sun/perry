**Workspace hygiene (base improvements):** adopt `[workspace.lints]` with a new `clippy` CI gate (deny-level lints only), a `cargo-deny` license/duplicate-version gate with root `deny.toml`, and unify 16 shared dependencies under `[workspace.dependencies]`. First clippy run fixed three latent findings (array sort NaN idiom, dgram u16 port comparison, dead loop in stream piping) and added the missing `license` field to the UI crates.
