perf(runtime): #6759 Phase C2 — per-key descriptor-presence summaries move into the per-object `ObjectMeta` record (two monotonic Bloom words). `get_property_attrs`/`get_accessor_descriptor` prove most misses in two loads (no `String` build, no table probe), the `Object.keys` fast path drops its O(table) owner scan, and `plain_data_write_may_intercept` refines the per-object descriptor flag per-key (an object with one `defineProperty`'d key keeps the dynamic-write fast path for its other keys). Also shields fresh objects at recycled addresses from dead owners' not-yet-pruned descriptor entries, and adds a nesting-safe `GcSuppressScope` no-move window around the meta allocation.
