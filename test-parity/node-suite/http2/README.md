# node:http2 granular parity suite

Focused deterministic cases for Perry's `node:http2` compatibility layer. Cases avoid external network dependencies and use loopback/self-closing servers where possible.
